const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import <%= containerName %> from './<%= containerFilename %>';

describe('<%= featureName %>.<%= containerName %>', () => {
  it('should do something', () => {
    
  });
});
